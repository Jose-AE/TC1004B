USE a_1;
-- @block
create table producto(
    id integer primary key auto_increment,
    name varchar(40),
    price integer,
    category varchar(30)
);
INSERT table producto (
        name varchar(40),
        price integer,
        category varchar(30)
    );